# Anthropic (Ticket 0.5 — AI client)
ANTHROPIC_API_KEY=

# Supabase (Ticket 0.4 / Ticket 1)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Ticket 1 — comma-separated allowlist
# Exact emails and/or domains: teacher@school.org,@school.org
ALLOWED_TEACHER_EMAILS=

# Google OAuth Client ID/Secret are configured in the Supabase dashboard
# (Authentication → Providers → Google), not in this Next.js app.
# You will create them in Google Cloud Console — see Ticket 1 setup notes.
